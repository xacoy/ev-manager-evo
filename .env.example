# Copia este archivo a ".env" y rellena tus propios valores antes de "docker compose up".
# Autenticación HTTP Basic para todo el sitio (recomendado siempre, incluso detrás de Cloudflare Access).
AUTH_USER=xaco
AUTH_PASS=cambia_esto_por_algo_fuerte
